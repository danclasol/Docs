# Binaries

### `BINARY(n)`

Binary data (bytes)

```sql
col_binary BINARY
```

### `VARBINARY(n)`

Variable-length binary

- `n`: maximum number of bytes
  - up to `8MB` per value

```sql
col_binary VARBINARY(n)
```
