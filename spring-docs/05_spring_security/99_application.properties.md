# Spring Security

### Set default user

Spring Boot can automatically create a default user when no custom UserDetailsService is defined.

```xml
spring.security.user.name=123
spring.security.user.password=abc
```